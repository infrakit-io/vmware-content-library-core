Release Notes

Unreleased
Highlights:

- TBD

Notes:

- TBD

v0.1.0 (2026-03-02)
Highlights:

- Initial public release with generic VMware content library primitives.
- Added idempotent library/item operations (`EnsureLibrary`, `EnsureItemFromURL`).
- Added concurrent-safe item import lock and OVF deploy wrapper (`DeployItem`).

Notes:

- Provider-agnostic API surface; no Talos-specific types or defaults.
