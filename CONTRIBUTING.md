# Contributing

1. Fork and create a feature branch from `master`.
2. Run `make fmt lint test` before opening a PR.
3. Keep the package reusable across VMware environments and based on explicit inputs.
